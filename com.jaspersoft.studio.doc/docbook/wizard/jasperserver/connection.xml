<?xml version="1.0" encoding="UTF-8"?>
<section version="5.0" xsi:schemaLocation="http://docbook.org/ns/docbook http://www.docbook.org/xml/5.0/xsd/docbook.xsd http://www.w3.org/1999/xlink http://www.docbook.org/xml/5.0/xsd/xlink.xsd"
	xml:base="../" xmlns="http://docbook.org/ns/docbook" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns:xlink="http://www.w3.org/1999/xlink"
	xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:ns="http://docbook.org/ns/docbook">
<title>Connect to JasperReports Server</title>
<section>
	  <para>
	  	JasperReports Server is a stand-alone and embeddable reporting server. In this tutorial we will see how connect JaspersoftStudio to JasperReports Server and how to upload and download a report from the server.
	  	Since we need to interact with JasperReports Server we need an URL to it and we need the credential to access it. Anyway if you doesn't have one of this you can do a local installation of JasperReports Server. 
	  	To do this, follow <ulink url="http://community.jaspersoft.com/project/jasperreports-server/releases">THIS</ulink> link and download the correct version for your operative system, and then install it following the on screen instructions.
	  </para>
	  <para>
	  	To have an easy replicable example lets suppose that we are using a local installation of JasperReports Server. To check if our server is running open your browser and copy in the address bar this URL: 
		<programlisting>http://localhost:8080/jasperserver/login.html</programlisting>
		You should see a window like this:
	  </para>
	  <figure>
		<title>JasperReports Server login screen</title>
		<screenshot>
			<mediaobject>
				<imageobject>
					<imagedata fileref="1.png" format="PNG" contentwidth="800px" />
				</imageobject>
			</mediaobject>
		</screenshot>
	  </figure>
	  <para>
	  	If it is all working, start Jaspersoft Studio, go into the "Repository Explorer" tab and hit the button "Create a JasperReports Server Connection":
	  </para>
	  <figure>
		<title>Create a connection to JasperReports server</title>
		<screenshot>
			<mediaobject>
				<imageobject>
					<imagedata fileref="2.png" format="PNG" contentwidth="800px" />
				</imageobject>
			</mediaobject>
		</screenshot>
	  </figure>
	  <para>
	  	A dialog to insert the data of the server will appear, fill it as follow:
		<itemizedlist mark='opencircle'>
			<listitem>Name: the name of the connection, you can use any name you want. For this example we will leave the default one: JasperReports Server;</listitem>
			<listitem>URL: the address of the server, the default one is already right if we are using a local server. For this example the correct address is http://localhost:8080/jasperserver/services/repository;</listitem>
			<listitem>User: the username to access the server, the default one for the local server is "jasperadmin";</listitem>
			<listitem>Password: as the username, for the local server by default it is "jasperadmin"</listitem>
		</itemizedlist>
		Then hit the test button to test the connection and it is all working hit the "Finish" button.
	  </para>
	  <figure>
		<title>Configure the connection</title>
		<screenshot>
			<mediaobject>
				<imageobject>
					<imagedata fileref="3.png" format="PNG" contentwidth="800px" />
				</imageobject>
			</mediaobject>
		</screenshot>
	  </figure>
	  <para>
	  	At this point you are connected to the server and you can upload and download reports.
	  </para>
      </section>
</section>